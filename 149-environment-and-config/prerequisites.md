# Prerequisites for 149-environment-and-config

## Before You Start

- [13-structs](../13-structs/skills.md) — config as a struct
- [22-modules-and-tooling](../22-modules-and-tooling/skills.md) — the flag package is part of the standard library

## You're Ready When You Can...

- [ ] Read an env var with `os.Getenv` and detect if it's set with `os.LookupEnv`
- [ ] Define flags with `flag.String/Int/Bool` and call `flag.Parse()`
- [ ] Build a `LoadConfig()` function that reads from env vars with defaults

## Next Steps

- [150-logging](../150-logging/README.md)
