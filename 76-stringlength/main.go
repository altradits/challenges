package main

import "fmt"

// StringLength returns the number of characters in a string.
// Do NOT use the built-in len() function.
func StringLength(s string) int {
	// TODO: Implement this function
	// Hint: Use a for...range loop to iterate over the string
	// and count each character manually.
	return 0
}

func main() {
	fmt.Println(StringLength("Hello"))
	fmt.Println(StringLength(""))
	fmt.Println(StringLength("Go is fun"))
}
