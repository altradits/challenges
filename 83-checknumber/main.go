package main

import "fmt"

// CheckNumber returns true if the string contains any digit (0-9), false otherwise.
func CheckNumber(s string) bool {
	// TODO: Implement this function
	// Hint: Iterate through each character.
	// If you find a digit (between '0' and '9'), return true immediately.
	// If the loop finishes without finding any, return false.
	return false
}

func main() {
	fmt.Println(CheckNumber("Hello"))
	fmt.Println(CheckNumber("Hello1"))
}
